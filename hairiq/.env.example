# Email signup (spec §12) — Mailchimp is the default provider.
# Until these are set, signups validate but are not stored (server logs a warning).
# API key must include the data-center suffix, e.g. abc123...-us21
MAILCHIMP_API_KEY=
MAILCHIMP_AUDIENCE_ID=
